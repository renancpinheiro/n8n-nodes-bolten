{
    "url": "/clients/api/v1/projects",
    "method": "GET",
    "response": {
        "iterate": "{{body.items}}",
        "output": {
            "label": "{{item.name}}",
            "value": "{{item.id}}"
        }
    }
}

{
    "url": "/clients/api/v1/projects/{{parameters.projectId}}/components",
    "method": "GET",
    "response": {
        "iterate": {
            "container": "{{body.items}}",
            "condition": "{{if(parameters.category, item.category = parameters.category, true)}}"
        },
        "output": {
            "label": "{{item.name}}",
            "value": "{{item.id}}"
        }
    }
}

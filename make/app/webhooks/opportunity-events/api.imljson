{
    "respond": {
        "status": 200,
        "type": "json",
        "body": {
            "ok": true
        }
    },
    "condition": "{{if(length(parameters.events) > 0, contains(parameters.events, body.type), true)}}",
    "output": "{{body}}"
}

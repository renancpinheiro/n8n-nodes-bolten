{
    "url": "/clients/api/v1/projects",
    "method": "GET",
    "response": {
        "iterate": "{{body}}",
        "output": {
            "label": "{{item.name}}",
            "value": "{{item.id}}"
        }
    }
}
